# TV Running Lines Monitor

Система мониторинга и анализа бегущих строк с телевизионных каналов. Проект автоматизирует процесс захвата, распознавания и анализа бегущих строк с различных телеканалов по заданной тематике (например, деятельность МЧС), сохраняя результаты в структурированном виде и отправляя их в Telegram через бота.

Мониторинг происходит как в автоматическом режиме (с помощью планировщика задач, который настроен под периоды появления "бегущих" новостных строк на телеканалах), так и в ручном (принудительном) - при нажатии на соответствующие кнопки интерфейса.

## 🚀 Основные возможности

- **Автоматический захват скриншотов** с телеканалов
- **Распознавание текста** из бегущих строк с помощью OCR (Tesseract + EasyOCR)
- **Обработка и анализ** распознанного текста
- **Фильтрация по ключевым словам** для поиска релевантной информации
- **Сохранение результатов** в Excel-файлы или JSON
- **Автоматическая отправка** результатов в Telegram
- **Планировщик задач** для регулярного мониторинга по расписанию lines
- **Поддержка множества телеканалов** (RBK, MIR24, RenTV, TVC, Звезда, Россия 1, Россия 24, Известия, 360 Подмосковье, Москва 24, НТВ)
- **Графический интерфейс** для управления процессом
- **Распознавание текста из видео** с созданием скриншотов каждые 10 секунд (для телеканалов RBK, MIR24, где строка движется справа налево)
- **Отправка crop-видео с ключевыми словами** в Telegram

## 🔧 Архитектура и рефакторинг

### RBKMIR24Manager - Централизованное управление записью

Система использует класс `RBKMIR24Manager` в модуле `rbk_mir24_parser.py` для централизованного управления записью и мониторингом каналов RBK и MIR24. Это решение устраняет дублирование логики и улучшает поддерживаемость кода.

#### Основные возможности менеджера:
- **Ручная запись crop-видео** - запуск записи по кнопке
- **Запланированная запись crop-видео** - автоматическая запись по расписанию
- **Мониторинг строк** - ручной и автоматический захват скриншотов
- **Остановка процессов** - корректное завершение всех операций
- **Очистка ресурсов** - освобождение памяти и потоков

#### Преимущества рефакторинга:
1. **Устранение дублирования** - вся логика управления записью в одном месте
2. **Улучшенная поддерживаемость** - изменения делаются только в менеджере
3. **Лучшая тестируемость** - менеджер можно тестировать независимо
4. **Четкое разделение ответственности** - каждый модуль отвечает за свою область
5. **Упрощение основного кода** - `main.py` стал более читаемым

#### Интеграция с основным приложением:
```python
# В main.py
from rbk_mir24_parser import RBKMIR24Manager

class MonitoringApp:
    def __init__(self):
        self.rbk_mir24_manager = RBKMIR24Manager(self, self.ui)
    
    def start_rbk_mir24(self):
        self.rbk_mir24_manager.start_manual_recording()
    
    def stop_rbk_mir24(self):
        self.rbk_mir24_manager.stop_recording()
```

## 📸 Процесс захвата бегущих строк

Система автоматически захватывает бегущие строки с телеканалов в реальном времени. Процесс включает следующие этапы:

1. **Подключение к видеопотоку** - система подключается к онлайн-трансляции канала
2. **Анализ кадров** - программа анализирует каждый кадр видео в поисках области с бегущей строкой
3. **Захват области** - при обнаружении бегущей строки система выделяет и сохраняет эту область как скриншот
4. **Распознавание текста** - с помощью OCR (оптическое распознавание символов) извлекается текст из скриншота
5. **Фильтрация и обработка** - текст проверяется на читаемость и обрабатывается для улучшения качества

![Пример захвата бегущей строки с Р24](Р24%20(1).png)

*Пример автоматического захвата бегущей строки с канала Россия 24*

## 🎥 Распознавание текста из видео

### Новая логика работы

Система работает в два этапа:
1. **Создание скриншотов**: Из видео каждые 10 секунд создаются скриншоты
2. **Распознавание текста**: Из созданных скриншотов распознается текст

Это позволяет:
- Лучше контролировать процесс распознавания
- Анализировать промежуточные результаты
- Экономить ресурсы при обработке длинных видео

### Отправка видео в Telegram

Система поддерживает автоматическую отправку видео с ключевыми словами в Telegram:

1. **Проверка ключевых слов**: После распознавания текста система проверяет наличие ключевых слов
2. **Подготовка к отправке**: Видео с найденными ключевыми словами помечаются для отправки
3. **Отправка**: При нажатии кнопки "Отправить в ТГ" отправляются только видео с ключевыми словами
4. **Очистка**: Все видео файлы удаляются после обработки

**Мониторинг строк:**
- **Запустить мониторинг** — запуск мониторинга строк (скриншотов) по всем каналам с crop.
- **Остановить мониторинг** — остановка мониторинга строк.
- **Сохранить и отправить строки** — обработка скриншотов, фильтрация по ключевым словам и отправка релевантных скриншотов в Telegram.

**RBK и MIR24:**
- **Запустить запись** — ручной запуск записи crop-видео для RBK и MIR24.
- **Остановить запись** — остановка ручной записи crop-видео.
- **Проверить и отправить crop-видео** — распознавание текста в crop-видео, поиск ключевых слов и отправка подходящих видео в Telegram.
- **Очистить кэш API** — очистка кэша результатов Hugging Face API.

**Планировщик:**
- **Приостановить** — приостановка автоматического расписания.
- **Возобновить** — возобновление автоматического расписания.


## 📋 Поддерживаемые каналы

- РБК
- МИР24
- РЕН ТВ
- ТВЦ
- Звезда
- Россия 1
- Россия 24 ("белые и синие" строки)
- Известия
- 360 Подмосковье
- Москва 24 и НТВ (необходимо перепроверять ссылки на видеопоток (ссылки периодически меняются))

## 🛠 Технические требования

### Системные требования
- Windows 10 или выше / Linux / macOS
- Python 3.8 или выше
- Минимум 4GB RAM
- Доступ в интернет
- Достаточно места на диске для хранения скриншотов и видео

### Архитектура системы

#### Централизованное управление конфигурацией
Система использует модуль `config_manager.py` для централизованного управления конфигурационными файлами:
- **Кэширование**: Конфигурация загружается один раз и кэшируется на 30 секунд
- **Автоматическое обновление**: При изменении файлов конфигурации кэш автоматически обновляется
- **Единая точка доступа**: Все модули используют один экземпляр для загрузки `channels.json` и `keywords.json`
- **Обратная совместимость**: Сохранены функции-обертки для совместимости с существующим кодом

#### Оптимизация Hugging Face API
Система включает оптимизированную работу с Hugging Face API для анализа ключевых слов:
- **Единый запрос**: Все ключевые слова проверяются одним запросом к API вместо отдельных запросов
- **Кэширование результатов**: Результаты API кэшируются для избежания повторных запросов
- **Автоматическая очистка**: Кэш автоматически очищается при переполнении (максимум 1000 записей)
- **Fallback на локальную проверку**: При недоступности API используется локальный алгоритм проверки
- **Управление кэшем**: Пользователь может очистить кэш через интерфейс

#### Основные модули
- `main.py` - главный модуль приложения с GUI
- `config_manager.py` - централизованное управление конфигурацией
- `UI.py` - графический интерфейс пользователя
- `parser_lines.py` - мониторинг и захват бегущих строк
- `rbk_mir24_parser.py` - запись и обработка видео
- `lines_to_csv.py` - обработка скриншотов и сохранение в CSV
- `telegram_sender.py` - отправка файлов в Telegram
- `utils.py` - вспомогательные функции

### Зависимости
Основные библиотеки:
- python-telegram-bot
- pandas
- opencv-python (заменяет FFmpeg для работы с видео)
- pytesseract
- easyocr
- pillow
- schedule
- tkinter
- openpyxl

### Важное изменение
**Проект больше не требует установки FFmpeg!** Все функции работы с видео (захват скриншотов, запись видео) теперь выполняются с помощью OpenCV, что делает проект более портативным и простым в установке.

## 🚀 Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/your-username/tv_running_lines.git
cd tv_running_lines
```

2. Создайте виртуальное окружение:
```bash
python -m venv .venv
```

3. Активируйте виртуальное окружение:
```bash
# Windows
.venv\Scripts\activate
# Linux/Mac
source .venv/bin/activate
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

5. Настройте конфигурацию:

Для работы приложения необходимо указать токены для Telegram и, опционально, для Hugging Face. Система поддерживает два способа конфигурации, которые проверяются в следующем порядке:

**Способ 1: Переменные окружения (рекомендуемый)**

Это самый безопасный способ, так как токены не хранятся в файлах проекта.

*   `TELEGRAM_TOKEN`: Токен вашего Telegram-бота.
*   `TELEGRAM_CHAT_IDS`: ID чатов для отправки сообщений, через запятую (например, `987654321,123456789`).
*   `HF_API_TOKEN` (опционально): Токен для Hugging Face API для улучшенного анализа текста.
*   `HF_TOKEN` (опционально): Токен для дополнительных функций Hugging Face.

**Способ 2: Файл `config.json`**

Если переменные окружения не заданы, система будет искать файл `config.json` в корневой папке проекта.

1.  Создайте файл `config.json` (можно скопировать `config.example.json`, если он есть).
2.  Заполните его вашими данными:

    ```json
    {
        "telegram_token": "YOUR_BOT_TOKEN_HERE",
        "chat_ids": [YOUR_telegram_id],
        "hf_api_token": "YOUR_HUGGING_FACE_API_TOKEN_HERE",
        "hf_token": "YOUR_HUGGING_FACE_TOKEN_HERE"
    }
    ```

**Важно:** Никогда не храните токены напрямую в коде (`telegram_sender.py` или других `.py` файлах). Система спроектирована так, чтобы считывать их из переменных окружения или `config.json`.

**Проверка конфигурации**

После настройки вы можете запустить скрипт `check_config.py`, чтобы убедиться, что все настроено правильно:
```bash
python check_config.py
```

## 💻 Использование

### Быстрый запуск через GUI (рекомендуется)

1. Запустите приложение:
```bash
python main.py
```

2. В открывшемся окне найдите раздел **"Проверка видео"**

3. Нажмите кнопку **"Проверка видео"** для запуска распознавания

4. Следите за статусом в интерфейсе:
   - "Выполняется" - процесс идет
   - "Завершено" - распознавание закончено
   - "Ошибка" - возникла проблема

5. Результаты автоматически сохраняются в папку `recognized_text/`

### Использование GUI

### Основные элементы управления
- **Видеонаблюдение**: 4 окна для отображения потоков с телеканалов. Каналы можно выбирать из выпадающего списка.
- **Управление строками**:
  - `Запуск строк`: ручной запуск создания скриншотов со всех каналов, где настроен `crop`.
  - `Стоп строки`: остановка создания скриншотов.
  - `Сохранить и отправить строки`: обработка папки `screenshots`, фильтрация по ключевым словам и отправка релевантных скриншотов в Telegram.
- **Управление crop-видео**:
  - `Запуск РБК и МИР24`: ручной запуск записи crop-видео.
  - `Стоп РБК и МИР24`: остановка ручной записи.
  - `Проверить и отправить crop-видео`: запуск обработки всех видео в папке `lines_video`, распознавание текста, поиск ключевых слов и отправка в Telegram.
- **Планировщик**:
  - `Пауза`/`Возобновить`: управление автоматическим запуском задач по расписанию из `channels.json`.
  - `Перезагрузить`: перезагрузка расписания из `channels.json` без перезапуска приложения.
- **Настройки (⚙️)**: открытие окна для редактирования, добавления и удаления каналов, их URL, расписания и других параметров.


### Настройка ключевых слов

Отредактируйте файл `keywords.json`:
```json
{
  "keywords": [
    "важное слово 1",
    "важное слово 2",
    "ключевая фраза"
  ]
}
```


## 📦 Создание исполняемого файла (.exe)

Проект настроен для сборки с помощью **PyInstaller** с использованием `.spec` файла, что гарантирует включение всех необходимых бинарных и данных файлов.

1.  **Установите PyInstaller**:
    ```bash
    pip install pyinstaller
    ```
2.  **Запустите сборку**:
    Выполните команду в корневой папке проекта:
    ```bash
    pyinstaller main.spec
    ```
3.  **Результат**:
    Готовый `.exe` файл и все сопутствующие данные будут находиться в папке `dist/Мониторинг строк МЧС`.

Файл `main.spec` уже настроен для включения:
- Бинарных файлов `tesseract.exe`, `ffmpeg.exe`, `ffprobe.exe`.
- Данных для Tesseract (`tessdata`).
- Файлов конфигурации `channels.json`, `keywords.json` и `config.json`.
- Иконки приложения.

## 📂 Структура проекта
```
tv_running_lines/
├── main.py                     # Главный файл приложения
├── UI.py                       # Графический интерфейс
├── telegram_sender.py          # Модуль отправки в Telegram
├── lines_to_csv.py             # Обработка и сохранение строк
├── parser_lines.py             # Парсер бегущих строк (скриншоты)
├── rbk_mir24_parser.py         # Менеджер записи видео и мониторинга
├── config_manager.py           # Управление конфигурацией
├── check_config.py             # Скрипт проверки конфигурации
├── utils.py                    # Вспомогательные функции
├── main.spec                   # Конфигурация для сборки EXE
├──
├── bin/                        # Бинарные файлы (Tesseract, FFmpeg)
│   ├── tessdata/
│   └── tesseract.exe
├── logs/                       # Папка для лог-файлов
├── screenshots/ 
    └── CHANNEL_NAME/               # Временная папка для сырых скриншотов
├── screenshots_processed/      # Обработанные скриншоты (с ключевыми словами)
├── lines_video/                # Папка для записанных crop-видео
│   └── CHANNEL_NAME/
├── channels.json               # Конфигурация каналов
├── keywords.json               # Ключевые слова для анализа
├── config.json                 # Пользовательская конфигурация (токены, таймауты)
├── requirements.txt            # Зависимости проекта
├── README.md                   # Эта документация
└── .gitignore                  # Файлы, игнорируемые Git
```

## ⚖️ Лицензия
Проект распространяется под лицензией MIT. См. файл `LICENSE` для получения дополнительной информации.

## ⚙️ Конфигурация каналов

### Структура файла channels.json

```json
{
  "RBK": {
    "url": "https://e2-online-video.rbc.ru/online2/rbctvhd_1080p/index.m3u8?e=e2&t=Izzi0I",
    "interval": "1/7",
    "crop": "crop=1500:45:330:960",
    "lines": [
      "00:00", "00:20", "00:40", "01:00", "01:20", "01:40", "02:00", "02:20", "02:40", "03:00",
      "03:20", "03:40", "04:00", "04:20", "04:40", "05:00", "05:20", "05:40", "06:00", "06:20",
      "06:40", "07:00", "07:20", "07:40", "08:00", "08:20", "08:40", "09:00", "09:20", "09:40",
      "10:00", "10:20", "10:40", "11:00", "11:20", "11:40", "12:00", "12:20", "12:40", "13:00",
      "13:20", "13:40", "14:00", "14:20", "14:40", "15:00", "15:20", "15:40", "16:00", "16:20",
      "16:40", "17:00", "17:20", "17:40", "18:00", "18:20", "18:40", "19:00", "19:20", "19:40",
      "20:00", "20:20", "20:40", "21:00", "21:20", "21:40", "22:00", "22:20", "22:40", "23:00",
      "23:20", "23:40"
    ],
    "default_duration": 10,
    "special_durations": {
      "17:00": 15
    }
  }
}
```

### Параметры конфигурации

- **url**: Ссылка на видеопоток канала
- **interval**: Интервал захвата скриншотов (например, "1/7" означает каждые 7 секунд)
- **crop**: Параметры обрезки области с бегущей строкой (формат: crop=width:height:x:y)
- **lines**: Массив времени для автоматического мониторинга и записи crop-видео (формат: "HH:MM")
- **default_duration**: Длительность записи по умолчанию в минутах
- **special_durations**: Особые длительности записи для конкретного времени

### Настройка времени мониторинга

Время в массиве `lines` указывает, когда система должна:
1. **Запустить мониторинг строк** (создание скриншотов)
2. **Записать crop-видео** (для RBK и MIR24)

Формат времени: "HH:MM" (например, "10:20", "14:30")

### Примеры настройки

#### Для каналов с бегущими строками (R1, Zvezda, TVC, RenTV, NTV):
```json
{
  "R1": {
    "url": "https://vgtrkregion-reg.cdnvideo.ru/vgtrk/0/russia1-hd/1080p.m3u8",
    "interval": "1/11",
    "crop": "crop=1420:80:500:1000",
    "lines": ["05:00", "05:30", "06:00", "06:30", "07:00", "07:30", "08:00", "08:30", "09:00"],
    "default_duration": 5
  }
}
```